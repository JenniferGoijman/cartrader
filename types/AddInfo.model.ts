export type AddInfo = {
  make: string;
  model: string;
  year: string;
  miles: string;
  price: string;
  city: string;
  seats: string;
  features: string;
  description: string;
  image: File | null;
}